# 改善候補（優先順位順）

現状の仕様ではなく、今後の改善候補を記録するメモです。仕様そのものは [`specification.md`](./specification.md) を参照してください。

2026-07-06 時点の実装を再調査し、データ保護・挙動不一致・回帰防止・操作性・性能・保守性の順で優先順位を付けています。完了した項目や現行実装と合わなくなった項目は残していません。

## 高優先度

### 1. 保存データ migration の責務整理

- `schemaVersion` は保存データへ追加済みだが、現状の `normalizeState` は `catalogVersion` と個別フィールドの有無を見ながら一括で後方互換を吸収している。
- 保存形式の変更が増えると、どの移行を適用したか、古い形式をいつまで扱うかが判別しにくくなる。
- 対策案: `schemaVersion` ごとの migration 関数を順番に適用してから現行形式を検証する。各世代の fixture を用意し、移行後の state と破損データの扱いをテストする。

### 2. 入力値と保存値の型分離

- `WorkoutSet.weight` / `recordValue` は、入力途中の文字列と計算用の数値を同じ `string | number` で表している。
- `number()`、`isBlank()`、表示・単位変換関数が境界を吸収しており、保存値の保証やバリデーションを追加するほど扱いが複雑になる。
- 対策案: 保存値は数値または明示的な空値へ正規化し、入力中の文字列は画面ローカル state に閉じ込める。既存の `localStorage` には migration を用意する。

## 中優先度

### 3. 履歴増加時の集計インデックス

- `buildPartRecentLabels` は部位ごとに全ワークアウトを `filter` / `sort` し、ホーム画面の集計や分析画面にも配列走査が残る。
- 現在のデータ量では問題になりにくいため、実データで計測してから日付・種目・部位別の索引や 1 回の走査へ置き換える。

### 4. UI フィードバックの堅牢化

- トーストは連続発火すると前の内容を上書きする。
- 削除操作には Undo を追加済みだが、連続削除すると直前のトーストだけが残るため、複数操作の復元余地はまだ弱い。
- 対策案: 通知キューを導入し、連続発火した通知や複数の Undo 候補を順番に扱えるようにする。

## 低優先度

### 5. 重複 UI と大規模画面の整理

- ホーム、バックアップ、アカウント管理などに確認ダイアログのマークアップが重複している。
- `HomeScreen`、`DetailScreen`、`BackupScreen` は表示と操作ロジックが大きく、変更時の確認範囲が広い。
- 対策案: 見た目を無理に統一せず、Dialog、日付セル、月移動、スワイプ行、バックアップ行など安定した単位から共通化する。画面は意味のあるセクション単位で分割する。
